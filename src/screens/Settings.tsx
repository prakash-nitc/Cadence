/** Placeholder. See SPEC §9 for what lands here and when. */
export function Settings() {
  return (
    <div>
      <h1 className="font-display text-2xl tracking-display text-text">Settings</h1>
      <p className="mt-1 text-sm text-muted">Thresholds, the non-negotiable gate and notifications. Built in session 6.</p>
    </div>
  );
}
