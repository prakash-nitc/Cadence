/** Placeholder. See SPEC §9 for what lands here and when. */
export function Plan() {
  return (
    <div>
      <h1 className="font-display text-2xl tracking-display text-text">Plan</h1>
      <p className="mt-1 text-sm text-muted">Log today and plan tomorrow. Built in session 4.</p>
    </div>
  );
}
