/** Placeholder. See SPEC §9 for what lands here and when. */
export function Progress() {
  return (
    <div>
      <h1 className="font-display text-2xl tracking-display text-text">Progress</h1>
      <p className="mt-1 text-sm text-muted">Week shape, pacing and milestones. Built in session 5.</p>
    </div>
  );
}
